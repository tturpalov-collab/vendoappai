# Скопируйте в analytics/.env и подставьте реальные значения.
# analytics/.env в .gitignore — секреты в репозиторий не попадают.

# --- SSH bastion ---
SSH_HOST=18.196.28.193
SSH_USER=ec2-user
# путь к приватному ключу (в кавычках, если есть пробелы)
SSH_KEY="/Users/timur/Library/Mobile Documents/com~apple~CloudDocs/keys/VendoAI/timurAI"

# --- RDS за туннелем ---
RDS_HOST=vendo-app.cvwxd0jliglu.eu-central-1.rds.amazonaws.com
RDS_PORT=5432

# --- локальный порт туннеля ---
LOCAL_PORT=54322

# --- доступ к БД ---
PGUSER=vendo_ai_reader
# ВАЖНО: одинарные кавычки — в пароле есть $ и -
PGPASSWORD='ЗАМЕНИТЬ_НА_ПАРОЛЬ'
PGDATABASE=postgres
