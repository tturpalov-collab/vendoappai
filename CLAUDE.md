# vendoappai — аналитика платежей

## Что это за задача

Аналитика по платежам из production-базы (PostgreSQL на AWS RDS).
Пользователь хочет, чтобы **данные собирал и анализировал ассистент**, а не
запускал скрипты руками. Не предлагайте ему «вот скрипт, запустите» — поднимайте
туннель, ходите в базу и делайте выводы сами, показывая пользователю результат.

## Доступ к базе

База в приватной сети, доступ только через SSH-туннель к bastion-хосту:

```
ssh -L 54322:vendo-app.cvwxd0jliglu.eu-central-1.rds.amazonaws.com:5432 ec2-user@18.196.28.193 -i <ключ>
```

Пользователь БД — `vendo_ai_reader`, **read-only**. Пароль и путь к ключу лежат
в `analytics/.env` (не в git). Если файла нет — попросите пользователя создать
его из `analytics/.env.example`.

> Важно: это **не работает** из Claude Code на вебе — там исходящий порт 22
> заблокирован сетевой политикой окружения, и `ssh` не установлен. Задача
> рассчитана на локальный запуск (CLI / десктоп-приложение).

## Инструменты (запускает ассистент, не пользователь)

Все скрипты самодостаточные: сами поднимают SSH-туннель, сами ставят `psycopg`,
работают из любой папки. Пароль берут из `PGPASSWORD`, `analytics/.env` или спрашивают.

| Скрипт | Что делает |
|---|---|
| `vendo_payments.py` | Разведка: находит нужную базу и платёжные таблицы, профилирует их |
| `vendo_analytics.py` | 18 разрезов: выручка, отказы, организации, города, автоматы, товары, карты, чек, часы, дни |
| `vendo_drilldown.py` | 17 разрезов детализации: потери по автоматам, зоны по техотказам, август и `CE` по организациям, темп запуска, не запущенные машины |
| `vendo_export.py` | Выгрузка в CSV: сырые платежи поквартально (под лимит 30 МБ) + витрины + телеметрия модулей |
| `vendo_org_report.py` | Сводка по одной организации за N месяцев: 17 разрезов. `ORG=tazizi MONTHS=3` |
| `vendo_ce_reconcile.py` | Сверка CE с прошлым снимком базы: было / стало / дельта по месяцам, организациям и терминалам |
| `vendo_daily.py` | Платежи и выручка по дням: скользящее среднее, неделя к неделе, месяц к месяцу на равных отрезках, разбор роста. `DAYS=90` |

Старые обёртки для `psql` (`tunnel.sh`, `db.sh`, `sql/*.sql`) оставлены на случай,
если на машине есть `psql`, но основной путь — питоновские скрипты.

## Если нет psql

`psql` на машине может отсутствовать (Homebrew недоступен из-за сетевых
ограничений — `raw.githubusercontent.com` режется). Не заставляйте пользователя
что-то ставить руками: разберитесь сами.

Порядок предпочтений:

1. `psql` — проверить `command -v psql`, а также
   `/Applications/Postgres.app/Contents/Versions/latest/bin/psql`.
2. Python + `psycopg`: `python3 -m pip install --user "psycopg[binary]"`
   (pypi.org обычно доступен). Туннель поднимается тем же `analytics/tunnel.sh`,
   а запросы идут через `psycopg.connect(host="127.0.0.1", port=54322, ...)`,
   параметры берутся из `analytics/.env`.

Во втором случае положите тонкую обёртку в `analytics/query.py`, чтобы
дальнейшие запросы шли одной командой, и обновите этот раздел.

## Порядок работы

1. Схема базы пока не разобрана — начните с `00_discover.sql` и разберитесь,
   где лежат платежи, какие статусы, валюты и связи с пользователями/заказами.
2. Запишите разобранную схему сюда, в раздел «Схема», чтобы следующие сессии
   не повторяли разведку.
3. Дальше — витрины под запросы пользователя: выручка по периодам, средний чек,
   конверсия, возвраты, разрезы по методам оплаты и когортам.
4. Готовые запросы складывайте в `analytics/sql/` с говорящими именами.

## Бизнес-контекст

Компания — **дистрибьютор** вендинговых автоматов в ОАЭ. В `vendotek_unit` лежит
**весь поставленный парк**, включая ещё не введённое в эксплуатацию железо.

- 1008 автоматов в справочнике, 290 когда-либо продавали, 267 торгуют за 30 дней.
- **718 автоматов без продаж — это не поломка, а запас**, но запас неоднородный:
  - **299 выходили на связь за последние 30 дней** и не продали ничего. Проверено
    группировкой по адресам: **164 из них стоят на трёх складских адресах**
    (`104 DAMASCUS STREET AL QUSAIS` — 87, `3 AL HILM 1 ST MUSAFFAH` — 49,
    `FIRST CLASS VENDING ALQUASIS` — 28), ещё 24 записаны на плейсхолдер `Dubai`,
    92 без адреса. Правдоподобных точек — 19. Это склад, а не установленные машины;
    оценка «451 тыс. AED/мес потенциала» была ошибочной.
  - **306 замолчали разом в декабре 2025** и больше не появлялись — похоже на
    партию, завезённую и убранную на склад.
  - остальные ~114 разбросаны по месяцам или без внятной отметки.
**Адрес НЕ является признаком развёртывания.** Поле не заполняется системно:
215 машин с пустым городом дают 5,37 млн AED — 86 % всей выручки. Использовать
`address` или `city`, чтобы понять, стоит машина на точке или на складе, нельзя.

Доказательства: **141 работающая машина делит один адрес**, а 46 машин с продажами
вообще без адреса приносят 2,08 млн AED — треть выручки. `address` здесь
регистрационный реквизит, а не место установки.

**Единственный надёжный признак развёрнутой машины — регулярные продажи:**
больше одного платежа в день означает, что терминал стоит на месте и работает.

Картина парка по активности за 30 дней:

| Платежей в день | Машин | Выручка за 30 дней |
|---|---|---|
| 20+ | 35 | 471 778 |
| 5–20 | 129 | 564 981 |
| 1–5 | 53 | 55 917 |
| меньше 1 | 50 | 16 860 |
| продаж нет | 23 | — |

**164 машины дают 93 % выручки.** Доля заполненного адреса по группам — 78–91 %,
с активностью не связана вовсе.

**23 машины торговали и встали**, но значимых из них четыре — у остальных
восемнадцати от 1 до 28 платежей за всю историю, это пусконаладка:

| Автомат | Точка | Платежей | Выручка | Молчит |
|---|---|---|---|---|
| `221400016865` | ASTER CEDAR | 1 807 | 14 894 | 38 дн. |
| `221400017152` | Bur Dubai | 1 688 | 12 465 | 84 дн. |
| `221400016265` | Al Ain Road | 449 | 4 947 | 56 дн. |
| `221400016179` | Dubai | 233 | 5 178 | 264 дн. |

У двух крупнейших `last_seen` на день-два позже последней продажи — машину
выключили или увезли, это логистика, а не софт. Порядок потерь — тысячи AED
в месяц, не десятки тысяч.

**В проде есть тестовый терминал:** `221400017147` (`IntersoftPOS Testing 5.4.2026`)
дал 6 367 платежей на 3 654 AED, средний чек 0,57. Прогоны лежат в общей
статистике и раздувают счётчик платежей — исключать.

### Авария марта 2026

Скачок отказов до 27 % в марте—апреле 2026 — следствие **аварии дата-центра AWS
в ОАЭ**: переезд в другой ЦОД прошёл тяжело, сетевые задачи разгребали ещё 2–3
месяца. Хвост виден в данных: 12,5 % отказов в мае, 12,3 % в июне, 10,4 % в июле,
6,6 % в августе.

Это объясняет структуру отказов: `CE` и пустой код ответа — сетевой уровень, а не
решение банка. Не интерпретировать их как отказы эмитента.

### Пустой код ответа — это НЕ всегда отказ

Терминалы `soco-br-of-majid-al-futtaim` стоят в **парке развлечений**, где у
посетителей свои карты MiFare с балансом для игровых аппаратов. Люди прикладывают
их к нашему POS по ошибке; POS видит неплатёжную карту и отклоняет её с пустым
`response_code`.

**Это не отказ в оплате и не потерянная выручка — покупки не было вовсе.**

- В августе 2 509 таких событий — 96 % всех пустых кодов по компании.
- За всю историю ориентировочно 26 700 из 27 792 пустых кодов.
- Отсюда «хронические 63,9 % отказов у soco в феврале, до аварии» — это не авария
  и не поломка, а ровный фон промахов по ридеру.

**Подпись промаха чистая и не привязана к организации:**

```sql
pos_entry_mode = 'CONTACTLESS' and coalesce(aid,'') = '' and coalesce(pan,'') = ''
```

Так помечено 26 065 событий — 98,7 % всех промахов. У настоящих оплат
`pos_entry_mode` всегда `EMV CONTACTLESS` или `QR CODE READ`; простой
`CONTACTLESS` с пустыми AID и PAN встречается только при чужой карте.
Фильтровать этим правилом, а не по имени организации — сработает на любой площадке.

**Любую метрику отказов считать с исключением этих событий**, иначе approve rate
компании занижен примерно на 3 п.п. (87,2 % вместо 90 с лишним).

Пересчёт с этой поправкой:

- базовая линия отказов — **3,4 %** (декабрь—февраль без soco), а не 7 %;
- март 19,37 %, апрель 21,73 %, май 5,67 %, июнь 8,20 %, июль 5,95 %;
- **август без soco — 3,49 % против 3,33 % в феврале: сеть восстановлена полностью**;
- потери за март—август — **330 684 AED**, это 23 919 несостоявшихся покупок
  и 5,3 % выручки за период (в августе недобор уже 883 AED, то есть его нет).

**Незакрытый хвост:** в июне 1 142 пустых кода пришли **не** из парка развлечений
(в мае — 1, в июле — 121, в августе — 105). Событие **платформенное, а не клиентское**:
399 у `modern-vending`, 338 у `tazizi`, 98 у `coffepoint` и далее по всем
организациям. Пик — неделя 22 июня, 1 848 пустых кодов. Причина не установлена.

### Сверка от 05.09.2026 — что изменилось

Прогон `vendo_ce_reconcile.py` на данных по 5 сентября:

- **Целостность подтверждена.** Все месяцы с октября 2025 по июль 2026 совпали
  с базовым снимком до единицы — досинхронизация задним числом ничего не переписала.
- **Август закрылся на 617** против 576 в снимке. Но снимок держал 26 дней:
  22,2 CE в день в начале месяца против **8,2 в день в последние пять дней** —
  хвост августа в 2,7 раза лучше начала.
- **Сентябрь: 69 CE за 5 дней**, из них **43 даёт один терминал `221400017033`**
  (62 % всех обрывов компании). Без него — 26 обрывов на 17 754 попытки, **0,146 %**.
- **`221400017033` не починен.** Август 176 → 205, сентябрь 43 попытки и 43 отказа,
  снова 100 %. Накопительно с начала августа 248 обрывов и ноль успешных оплат.
  Он стоял первым приоритетом в наряде на проверку — судя по данным, выезда не было.

**Tazizi: набор терминалов с CE полностью меняется от месяца к месяцу.**
Июнь — 12 терминалов, июль — 6, август — 3, сентябрь — 0. Ни один не показывает
устойчивой картины: `MAF Ghaf Woods` 0 → 13 → 6 → 0, `L'Oreal` 1 → 27 → 0 → 0,
`Cognita` 16 → 0 → 0 → 0. Это подпись уходящей сетевой проблемы, а не плохих машин.
Прежняя догадка, что `Nabors Jafza` — недоведённая установка, **не подтвердилась**:
4 обрыва в первый месяц работы и ноль с тех пор.

**Расхождение в атрибуции:** терминал `221400016432` (`Haus & Haus`) в квартальном
отчёте Tazizi числится за `tazizi` (первая продажа 3 августа, 13 169 AED), а в разрезе
CE идёт под `vendotek-uae` с 20 обрывами. Либо терминал передали между организациями,
либо `organization_name` в `vendotek_vend` проставляется непоследовательно.
Влияет на атрибуцию и выручки, и ошибок — проверить.

### Подневная динамика на 05.09.2026

Прогон `vendo_daily.py` (данные по 5 сентября 13:57 — **день обрезан, читать по 4-е**).

- **4 сентября нормальное:** 4 060 платежей, 43 138 AED, 223 терминала, отказов 2,88 %.
  Рекорд по числу платежей для пятницы.
- **Сентябрь быстрее августа.** Будни 31.08–04.09 — 45 025 AED/день против 34 853
  (24–28 авг). С поправкой на день недели 41 888 против 34 971 в начале августа,
  **+19,8 %**; к среднему дню августа целиком (36 782) — +14 %. Проекция месяца ≈ 1,26 млн
  против 1,14 млн. Сравнение «первых N дней» в лоб даёт +12,7 % и врёт: у августа в
  отрезок попали суббота с воскресеньем, у сентября — только обрезанная суббота.
- **Растёт объём, а не чек.** Первые 5 дней: платежи 12 407 → 17 888 (+44 %),
  выручка +12,7 %, **средний чек 14,48 → 11,11 (−23 %)**. Перелом около 17 августа,
  одновременно с выходом рекордной августовской когорты (40 запусков). Выручка на
  терминал по месяцам снижается: 4 801 (май) → 4 537 → 4 523 → 4 223 (август).
- **Весь рост — от новых машин.** Равные отрезки: новые 48 терминалов +21 930,
  работавшие в обоих месяцах 208 штук +5 133 (**+3,1 %**), замолчавшие 10 штук −5 243.
  Прирост действующего парка ровно съедается выпавшими. Как только темп запусков
  встанет, встанет и выручка. Всего когда-либо продавали 292 терминала.
- **upay — 52 % выручки компании и при этом −10,4 %** (112 430 → 100 715) при парке
  74 → 76. 11 из 15 худших терминалов — его: `221400016276` −3 465 (обнулился),
  `221400017025` −2 785, `221400016287` −1 535. Главный риск.
  Рост дали tazizi +52 %, modern-vending +213 % (14 → 38 терминалов), coffepoint +290 %.
  soco −47 % на том же парке — похоже на конец школьных каникул в парке развлечений.
- **Атрибуция снова расходится:** `vendotek-uae` 12 терминалов → 3, а выручка втрое выше.
  Та же болезнь, что с `221400016432` — `organization_name` в `vendotek_vend`
  переставляется. Выручку по организациям без сверки не считать.
- **Связь в норме:** отказы 2,18–2,99 % против 3,49 % в августе и 3,33 % в феврале
  (до аварии). Техотказов 3–54 в день против 24–120 в конце июля.

**Две аномалии, причина не установлена:**

- **10–11 августа** — 71 897 и 61 431 AED при чеке 18,31 / 17,35, вдвое выше соседних дней.
- **28 августа** — провал до 2 630 платежей и 183 активных терминалов против 212 накануне
  и 189 назавтра: 29 машин выпали ровно на день.

### Базовый снимок для сверки CE (26.08.2026, данные по 26 августа)

Зашит в `vendo_ce_reconcile.py`, чтобы следующий прогон показывал дельту,
а не просто новые числа. Ключевые значения:

- платежей в базе 535 236, последний платёж 2026-08-26;
- CE по месяцам: окт 39, ноя 13, дек 109, янв 63, фев 45, **март 6 251,
  апрель 10 932**, май 1 629, июнь 2 496, июль 2 061, **август 576** (неполный);
- август по организациям: upay 401, modern-vending 79, ginco 27, vendotek-uae 20,
  awafi 15, alameed 12, tazizi 10, coffepoint 5, mania 5, fuelup 1, soco 1;
- август, топ терминалов: `221400017033` 176, `221400014242` 43, `221400017027` 38,
  `221400016790` 29, `221400017144` 24;
- tazizi по месяцам: июнь 59, июль 71, август 10 — в августе всё на двух
  терминалах: `221400010864` (6) и `221400014005` (4).

**Август в снимке неполный** — он закрыт 26-м числом, поэтому при следующем
прогоне итог за месяц вырастет закономерно, а не из-за ухудшения.

### Что уже выяснено по отказам

- **Сеть починена.** `CE` упал с 847 за неделю (пик 15 июня) до 37 (неделя 17 августа),
  −96 %. За август 574 против 10 932 в апреле. Последствия аварии AWS закрыты.
- **Пустой код ответа вниз не идёт** — но это и не сбой, см. раздел выше про MiFare.
- **Автомат `221400017033`** — 790 попыток, 790 технических отказов, ноль продаж
  за всю историю. Неисправный терминал, а не связь. В августе дал 174 `CE` — 30 %
  всех обрывов по компании.
- **20 игровых автоматов** (`PINBALL`, `AIR HOCKEY`, `NBA HOOPS`, `ON TARGET`,
  `KICK AND PUNCH`) с чеком ровно 12,00 AED и долей техотказов 46–80 %.
  Суммарно 329 425 AED. Одинаковый чек и одинаковая болезнь — общая причина.
- **Зоны по доле техотказов:** 23 автомата свыше 30 % дают 358 492 AED потерь;
  63 автомата в 5–15 % дают ещё 432 081 AED; 124 машины ниже 5 % — 60 тыс., их не трогать.

### Темп запуска и потенциал

25,7 машины в месяц за последние полгода (август — рекорд, 37). На оставшиеся 718
при таком темпе нужно 28 месяцев. Типичная машина приносит **≈1 500 AED в месяц
(медиана июля)**, среднее 4 542 завышено перекосом — планировать по медиане:
718 машин дадут порядка 1,1 млн AED в месяц, а не 3,3 млн.

### Ещё одна ловушка

`vendotek_unit.last_seen_at` хранит **секунды** эпохи, а не миллисекунды.
Делить на 1000 нельзя — получится январь 1970-го. В скриптах стоит проверка
`case when last_seen_at > 100000000000 then /1000 else as is end`.
Для `vendotek_module.status_changed_at` формат не проверен.

### Классификация отказов

Технический отказ (терминал не получил внятного ответа от хоста):
`response_code` пустой, `CE`, `TO`, `907`, `909`, `911`.
Банковский отказ: всё остальное, прежде всего `116` (недостаточно средств).
За весь период: 54 830 технических против 13 058 банковских — 81 % против 19 %.

## Схема

База `app` на сервере (не `postgres` — та пустая), PostgreSQL 18.3, схема `public`,
16 таблиц с префиксом `vendotek_`. Данные — зеркало внешней системы Vendotek:
почти везде есть `synced_at`, у части таблиц `dirty` / `external`.

**Предметная область: вендинговые автоматы в ОАЭ.** Валюта во всех продажах — `AED`,
организация `vendotek-uae`. Цены в `vendotek_planogram_product.price` — `bigint`,
предположительно в филсах (1 AED = 100 fils), проверять перед использованием.

### Платежи и продажи

| Таблица | Строк | Роль |
|---|---|---|
| `vendotek_payment` | 535 224 | **Факт-таблица платежей.** `cash_amount`, `cashless_amount`, `approved`, `name` (SALE), `pos_localtime_at` |
| `vendotek_payment_cashless` | 467 044 | Детализация безнала: `amount`, `pan` (маскированный), `application_label`, `pos_entry_mode`, `response_code`, `rrn`, `auth_id`, `issuer`, `transaction_duration_s`, `vend_duration_s` |
| `vendotek_payment_cash` | 68 180 | Детализация наличных: `amount`, `type` = CASH |
| `vendotek_vend` | 531 075 | Выдача товара: `unit_id`, `product_id`, `product_name`, `completed`, `cancelled`, `currency`, `terminal_id`, `organization_name` |
| `vendotek_vend_planogram_product_link` | 385 268 | Связь продажи с товарами |
| `vendotek_vend_fiscal` | мало | Фискализация: `qr`, `address`, `place` |

### Справочники и телеметрия

| Таблица | Строк | Роль |
|---|---|---|
| `vendotek_unit` | 1 008 | Автоматы: `sn`, `tid`, `location_name`, `address`, `city`, `region`, `country`, `tz` |
| `vendotek_org` | 27 | Организации, `distributor_id` |
| `vendotek_planogram_product` | 54 935 | Товары: `name`, `price`, `vat`, `code`, `gtin` |
| `vendotek_planogram`, `..._planogram_product_link` | мало | Планограммы автоматов |
| `vendotek_module`, `..._module_detail`, `..._module_link` | 6 435 / 17 878 / 3 432 | Телеметрия модулей автоматов |
| `databasechangelog`, `databasechangeloglock` | 15 / — | Служебные (Liquibase), в аналитике не участвуют |

### Связи

**Внешних ключей в базе нет** — связи только логические, по `uuid`:

```
vendotek_payment.vend_id           -> vendotek_vend.id
vendotek_payment_cash.payment_id   -> vendotek_payment.id
vendotek_payment_cashless.payment_id -> vendotek_payment.id
vendotek_vend.unit_id              -> vendotek_unit.id
vendotek_unit.owned_by_org_id      -> vendotek_org.id
vendotek_vend_planogram_product_link.vend_id -> vendotek_vend.id
```

Проверка сходимости: 68 180 (cash) + 467 044 (cashless) = 535 224 = ровно число
платежей. То есть у каждого платежа ровно одна строка детализации.

### Ловушки — читать перед любым запросом

1. **`synced_at` — это НЕ дата операции**, а момент выгрузки из внешней системы
   (вся выгрузка приходится на 19–25 августа 2026). Агрегировать по нему нельзя:
   получится вся выручка в одном месяце. Реальное время операции —
   `vendotek_payment.pos_localtime_at` (`timestamp without time zone`, локальное
   время терминала; смещение в `pos_localtime_offset_s`). Диапазон данных:
   **2025-05-06 … 2026-08-25**.
2. **Сумма платежа = `cash_amount + cashless_amount`.** По отдельности каждое поле
   заполнено только для своего типа оплаты, у остальных ноль — средний чек по одной
   колонке занижен в разы.
3. **Статусов-enum нет.** Успех платежа — булев `approved`; у безнала дополнительно
   `response_code` (`000` = успех). Отмена продажи — `vendotek_vend.cancelled`.
4. В ранних данных (май–сентябрь 2025) много транзакций на 0.01–0.04 AED — похоже на
   тестовые прогоны. Для бизнес-метрик их стоит отфильтровывать.

## Отчёты для клиентов

**Никаких сравнений с другими организациями.** В клиентском отчёте не должно быть
ни «лучший показатель в компании», ни «против 0,52 % в среднем по парку», ни
упоминаний, что у других клиентов было так же или хуже. Клиент видит только свои
цифры и свою динамику; бенчмарки по парку — это внутренняя информация.

Формулировки-заменители: вместо «лучше остальных» — абсолютное значение и динамика
к предыдущему месяцу; вместо «у всех организаций был всплеск» — «событие на стороне
платформы, а не на точках клиента».


## Правила

- Учётка read-only: только `SELECT`. Ничего не писать и не менять в базе.
- Секреты (`.env`, ключи) и выгрузки из `analytics/out/` — в `.gitignore`,
  в репозиторий не коммитить.
- На больших таблицах ставить `LIMIT` при разведке; для агрегатов сначала
  проверять объём через оценку строк.
